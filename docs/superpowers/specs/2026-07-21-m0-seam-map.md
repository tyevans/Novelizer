# M0 Substrate Seam Map

## Canon Core

### `novelizer/canon/event_store.py`

- `EventStore.__init__` (event_store.py:37-46) — generic-as-is — takes only a filesystem `path`; no fiction import.
- `EventStore.init` (event_store.py:48-56) — generic-as-is — creates a generic `events` table keyed on `(sequence, id, event_type, aggregate_id, payload, created_at, run_id)`; no `EventType` or fiction payload import anywhere in the file.
- `EventStore.close` (event_store.py:58-60) — generic-as-is — closes the connection, no domain coupling.
- `EventStore._insert` (event_store.py:62-83) — generic-as-is — inserts `(event_type: str, aggregate_id: str, payload_json: str, run_id)`; purely string/JSON shaped.
- `EventStore.append` (event_store.py:85-87) — generic-as-is — takes `event_type: str, aggregate_id: str, payload: BaseModel`; `BaseModel` here is generic pydantic, not a fiction-specific payload class.
- `EventStore.append_raw` (event_store.py:89-92) — generic-as-is — takes `event_type: str, aggregate_id: str, payload: dict`.
- `EventStore.events_for_run` (event_store.py:94-98) — generic-as-is — filters by `run_id: str`.
- `EventStore.events_tail` (event_store.py:100-106) — generic-as-is — takes `limit: int`.
- `EventStore.events_since` (event_store.py:108-121) — generic-as-is — takes `sequence: int, event_types: list[str] | None`; the `event_types` filter is caller-supplied strings, not an imported fiction registry.
- `EventStore.events_for_aggregate` (event_store.py:123-128) — generic-as-is — takes `aggregate_id: str`.
- `_row_to_event` (event_store.py:28-33) — generic-as-is — maps a raw DB row to `StoredEvent` (itself generic-as-is, see below); no fiction import.
- `_CREATE` schema string (event_store.py:13-23) — generic-as-is — column names (`sequence, id, event_type, aggregate_id, payload, created_at`) are domain-neutral event-sourcing primitives, not fiction table/column names.

Confirmed by import check: `event_store.py` imports only `novelizer.canon.db` and `novelizer.canon.events.StoredEvent` — no `EventType` import, no fiction Pydantic payload model import. `StoredEvent` itself (defined in events.py) is domain-neutral (see below), so the one cross-file dependency does not pull in fiction concepts.

### `novelizer/canon/events.py`

- `EventType` (events.py:6-63) — fiction-only — every one of its ~55 string constants (`WORLD_ENTRY_CREATED`, `BLUEPRINT_ADOPTED`, `THREAD_PLANTED`, `ARC_RESOLVED`, etc.) names a fiction-writing concept; the class's entire purpose is to be the fiction domain's event-type registry.
- `StoredEvent` (events.py:65-72) — generic-as-is — fields are `sequence: int, id: str, event_type: str, aggregate_id: str, payload: dict[str, Any], created_at: str, run_id: str | None`; no fiction-specific field.
- `AgentRemark` (events.py:75-83) — fiction-only — payload for `agent.remarked`, a fiction-room feed line (`agent_name`, `note`).
- `ThreadPlanted` (events.py:86-100) — fiction-only — payload for `thread.planted`, mints a narrative-thread identity.
- `ThreadTouched` (events.py:103-115) — fiction-only — payload for `thread.touched`.
- `ThreadPaidOff` (events.py:117-132) — fiction-only — payload for `thread.paid_off`.
- `ThreadAbandoned` (events.py:134-146) — fiction-only — payload for `thread.abandoned`.
- `ThemeIntroduced` (events.py:148-165) — fiction-only — payload for `theme.introduced`.
- `ThemeDeveloped` (events.py:168-182) — fiction-only — payload for `theme.developed`.
- `SecretCreated` (events.py:185-199) — fiction-only — payload for `secret.created`.
- `SecretLearned` (events.py:201-217) — fiction-only — payload for `secret.learned`, references `character_id`.
- `SecretReferenced` (events.py:219-236) — fiction-only — payload for `secret.referenced`.
- `SecretRevealed` (events.py:238-252) — fiction-only — payload for `secret.revealed`.
- `CausalEdgeDeclared` (events.py:254-270) — fiction-only — payload for `causal_edge.declared`, references `cause_chapter_id`/`effect_chapter_id`.
- `AnnotationStructureScored` (events.py:272-281) — fiction-only — payload for `annotation.structure_scored`, a Structure Analyst tension/pacing score.
- `ChapterRevised` (events.py:284-296) — fiction-only — payload for `chapter.revised`.
- `ChapterMined` (events.py:299-307) — fiction-only — payload for `chapter.mined`.
- `ChatUserMessaged` (events.py:310-319) — fiction-only — payload for `chat.user_messaged`.
- `ChatAgentReplied` (events.py:322-333) — fiction-only — payload for `chat.agent_replied`.
- `_default_authority` (events.py:336-338) — fiction-only — returns defaults keyed on fiction inspiration kinds (`names`, `professions`, `settings`, `beats`).
- `InspirationDrawn` (events.py:341-359) — fiction-only — payload for `inspiration.drawn` (Muse PRNG hand).
- `InspirationHandConsumed` (events.py:362-369) — fiction-only — payload for `inspiration.hand_consumed`.
- `InspirationHandSuperseded` (events.py:372-377) — fiction-only — payload for `inspiration.hand_superseded`.
- `InspirationUptakeRecorded` (events.py:380-389) — fiction-only — payload for `inspiration.uptake_recorded`.
- `PromiseMade` (events.py:392-416) — fiction-only — payload for `promise.made` (Chekhov's-gun tracking).
- `PromiseProgressed` (events.py:419-426) — fiction-only — payload for `promise.progressed`.
- `PromisePaid` (events.py:429-437) — fiction-only — payload for `promise.paid`.
- `PromiseReleased` (events.py:440-448) — fiction-only — payload for `promise.released`.
- `ThreadResolutionPlanned` (events.py:451-463) — fiction-only — payload for `thread.resolution_planned`.
- `SecretRevealPlanned` (events.py:466-474) — fiction-only — payload for `secret.reveal_planned`.
- `BeatSpec` (events.py:477-486) — fiction-only — a structural blueprint beat (`beat_id`, `ideal_pct`, `expected_polarity`).
- `BlueprintAdopted` (events.py:489-512) — fiction-only — payload for `blueprint.adopted`, mints the book's structural frame.
- `BlueprintRetargeted` (events.py:515-521) — fiction-only — payload for `blueprint.retargeted`.
- `BeatFulfilled` (events.py:524-532) — fiction-only — payload for `beat.fulfilled`.
- `ChapterBriefDrafted` (events.py:535-554) — fiction-only — payload for `chapter_brief.drafted` (Plotter's assignment to the Author).
- `ChapterBriefSuperseded` (events.py:557-562) — fiction-only — payload for `chapter_brief.superseded`.
- `ChapterBriefFulfilled` (events.py:565-570) — fiction-only — payload for `chapter_brief.fulfilled`.
- `ArcDeclared` (events.py:573-589) — fiction-only — payload for `arc.declared` (character arc: `ghost`, `lie`, `truth`, `want`, `need`).
- `ArcPivotPlanned` (events.py:592-598) — fiction-only — payload for `arc.pivot_planned`.
- `ArcAdvanced` (events.py:601-606) — fiction-only — payload for `arc.advanced`.
- `ArcResolved` (events.py:609-617) — fiction-only — payload for `arc.resolved`.
- `BookCompleted` (events.py:620-633) — fiction-only — payload for `book.completed`, declares the blueprint satisfied.

### `novelizer/canon/autonomy.py`

- `AutonomyLevel` (autonomy.py:16-20) — generic-as-is — `full_auto|gated_retcons|gated_canon|gated_all`; no fiction-specific member, purely a gating-strictness enum.
- `ProposalStatus` (autonomy.py:23-26) — generic-as-is — `open|approved|rejected`; domain-neutral workflow states.
- `Proposal` (autonomy.py:29-36) — generic-as-is — fields are `id, created_at, proposing_agent, target_event_type: str, target_aggregate_id: str, payload: dict, status`; no fiction-specific field, references event type/aggregate id only as opaque strings.
- `AutonomyState` (autonomy.py:39-44) — generic-as-is — `global_level`, `overrides: dict[str, AutonomyLevel]`, `level_for(agent_name)`; keyed by agent name string, no fiction concept.

### `novelizer/canon/policy.py`

- `_RETCON_EVENTS` (policy.py:5) — needs-generalization — literal set of fiction `EventType` constants (`WORLD_ENTRY_SUPERSEDED`, `FLAG_RESOLVED`).
- `_CANON_EVENTS` (policy.py:6-14) — needs-generalization — literal set of fiction `EventType` constants (`WORLD_ENTRY_CREATED`, `CHARACTER_CREATED`, `CHARACTER_UPDATED`, `CHAPTER_CREATED`, `CHAPTER_STATUS_CHANGED`, `CHAPTER_REVISED`, `SECRET_REVEALED`, plus `_RETCON_EVENTS`).
- `_ALWAYS_GATED` (policy.py:15) — needs-generalization — literal set containing `EventType.BLUEPRINT_ADOPTED`.
- `_NEVER_GATED` (policy.py:16-55) — needs-generalization — literal set of ~30 fiction `EventType` constants (`THREAD_PLANTED`, `ARC_RESOLVED`, `PROMISE_MADE`, etc).
- `_GATED_SETS` (policy.py:57-62) — needs-generalization — dict mapping `AutonomyLevel` to the above fiction-specific sets.
- `AutonomyPolicy.__init__` (policy.py:68-69) — generic-as-is — stores a `read_store` handle only.
- `AutonomyPolicy.is_gated` (policy.py:71-82) — generic-as-is — the method body only calls `state.level_for(agent_name)` and does set-membership checks against `event_type: str`; the mechanism takes no fiction-specific parameter (the fiction coupling lives in the module-level sets it reads, not in the method logic itself).

### `novelizer/canon/committer.py`

- `Committer` (committer.py:9-21) — generic-as-is — a single seam class holding an `EventStore`; no fiction import used within the class body.
- `Committer.commit` (committer.py:20-21) — generic-as-is — signature `(agent_name: str, event_type: str, aggregate_id: str, payload: BaseModel)`; appends directly via `EventStore.append`, no `EventType` or fiction payload reference.
- `GatingCommitter` (committer.py:24-46) — generic-as-is (mechanism) / needs-generalization (one literal reference) — the class shape (wraps `EventStore` + a `policy`) is domain-neutral, but its `commit` method (committer.py:35-46) references `EventType.PROPOSAL_CREATED` by literal string when routing a gated call into the proposal queue. The gating/proposal *mechanism* generalizes; the specific constant it imports from the fiction `EventType` registry (`novelizer.canon.events.EventType`) does not move as-is — a generalized substrate needs its own generic "proposal created" event-type constant that this class would reference instead.

## Projections

### `novelizer/canon_fs/backend.py`

- `READ_ONLY_ERROR` (backend.py:27-30) — generic-as-is — a domain-neutral read-only-refusal message string; no fiction reference.
- `KIND_DIRS` (backend.py:32) — needs-generalization — literal tuple `("chapters", "characters", "world", "threads", "secrets", "themes")`; this is exactly the fiction domain's entity-kind vocabulary hardcoded as the virtual filesystem's top-level directories.
- `_Snapshot` (backend.py:36-47) — mixed: the dataclass *shape* (an `index` plus one dict per entity kind, assembled once per call) is generic-as-is — "one consistent view of canon for a single backend call" is a domain-neutral pattern — but its field names (`chapters`, `characters`, `world`, `threads`, `secrets`, `themes`, `matrix`) are fiction-specific and would need to become a generic mapping keyed by kind for another domain.
- `CanonBackend.__init__` (backend.py:59-60) — generic-as-is — stores only a `read_store` handle.
- `CanonBackend.write/edit/upload_files/download_files` (backend.py:64-77) — generic-as-is — refuse with the domain-neutral `READ_ONLY_ERROR`; no fiction coupling.
- `CanonBackend.ls/read/grep/glob` (sync stubs, backend.py:81-91) — generic-as-is — raise `NotImplementedError` pointing at the async variant; no domain content.
- `CanonBackend._snapshot` (backend.py:93-110) — needs-generalization — directly calls `self._read.list_chapters()/list_characters()/list_world_entries()/list_threads()/list_secrets()/list_themes()/knowledge_matrix()` by name; the *pattern* (snapshot once, hand the same view to every render call in this request) is generic-as-is, but the six hardcoded method names and the `matrix` special case are fiction-domain-specific.
- `CanonBackend._render` (backend.py:112-127) — needs-generalization — a hardcoded `if kind == "chapter": render_chapter(...)` dispatch chain, directly importing `render_chapter/render_character/render_world_entry/render_thread/render_secret/render_theme` from `canon_fs/render.py`. Per the brief's anchor, this confirms the dispatch is generic-as-is *only if* it delegated to injected render functions — it does not; it imports and calls fiction render functions by name, so the dispatch itself is needs-generalization (a generalized substrate would take a `dict[str, RenderFn]` or similar injected mapping instead of importing named functions).
- `CanonBackend.aread` (backend.py:129-141) — generic-as-is — looks up `file_path` in `snap.index`, calls `self._render(snap, kind, record_id)`, slices via `slice_read_response`; the method body itself is domain-neutral (the fiction coupling is pushed into `_render` and `KIND_DIRS`, called from here).
- `CanonBackend.als` (backend.py:143-158) — needs-generalization — the top-level listing hardcodes `KIND_DIRS` as the root directory set and error message; mechanism (index-prefix matching for subdirectory listing) is otherwise generic-as-is.
- `CanonBackend.aglob` (backend.py:160-174) — generic-as-is — pure path/glob matching against `snap.index`; no fiction reference.
- `CanonBackend._glob_ok` (backend.py:176-180) — generic-as-is — pure path/glob matching helper.
- `CanonBackend.agrep` (backend.py:182-205) — generic-as-is (mechanism) — candidate selection over `snap.index` is domain-neutral, but it calls `self._render(snap, kind, record_id)` per candidate, so it inherits `_render`'s needs-generalization dependency at the call site.

### `novelizer/canon_fs/render.py`

- `_frontmatter` (render.py:6-9) — generic-as-is — takes `pairs: list[tuple[str, str]]`, formats YAML-ish frontmatter; no fiction reference. (Documented in outline_render.py:15-21 as being hand-copied rather than imported, since it's underscore-private — a real substrate extraction would promote this to a shared public helper.)
- `render_chapter` (render.py:12-19) — fiction-only — renders a `Chapter` model's title/prose/character_ids/editorial_status.
- `render_world_entry` (render.py:22-30) — fiction-only — renders a `WorldEntry`'s domain/canon_status/tags/body.
- `render_character` (render.py:33-58) — fiction-only — renders a `Character` plus knowledge-matrix-derived "Knows" section (calls `novelizer.canon.secrets.knowledge_cell_state`).
- `render_thread` (render.py:61-70) — fiction-only — renders a `ThreadRecord`'s state/touch_count/last_note.
- `render_secret` (render.py:73-86) — fiction-only — renders a `SecretRecord` plus knowledge-matrix-derived "known to" line.
- `render_theme` (render.py:89-97) — fiction-only — renders a `ThemeRecord`'s touch_count/last_note.
- Per the brief's anchor: each `render_*` function is fiction-only content, but the *shape* they share — a pure function taking (canon snapshot fields for one entity, optionally cross-referenced matrix/related-record data) and returning a rendered markdown-with-frontmatter string — is the pattern a domain's own render functions would replicate one-per-entity-kind.

### `novelizer/canon_fs/outline_render.py`

- `NO_BLUEPRINT_BODY` (outline_render.py:12) — fiction-only — the literal string "No blueprint adopted."
- `_frontmatter` (outline_render.py:18-21) — generic-as-is — verbatim duplicate of render.py's helper (see note above).
- `render_blueprint` (outline_render.py:24-55) — fiction-only — renders a `BlueprintRecord`/`BeatRecord` list; calls fiction-specific `novelizer.canon.beat_templates.beat_window`.
- `render_beats` (outline_render.py:58-83) — fiction-only — renders beats with drift annotations via `novelizer.brain.beat_drift.beat_drifts`.
- `render_brief` (outline_render.py:86-108) — fiction-only — renders a `ChapterBriefRecord`.
- `render_threads_plan` (outline_render.py:111-125) — fiction-only — renders active `ThreadRecord`s, filtering by `novelizer.canon.threads.TERMINAL_STATES`.
- `render_ledger` (outline_render.py:128-146) — fiction-only — renders `PromiseRecord`s with overdue detection via `novelizer.brain.ledger.overdue_promises`.
- Same shape observation as render.py applies here: each function is fiction-only, but the shared pattern (pure render function over one outline-concept's records, composing shared `_frontmatter`) generalizes.

### `novelizer/canon_fs/paths.py`

- `slugify` (paths.py:11-14) — generic-as-is — pure text-to-slug transform; no fiction reference.
- `_claim` (paths.py:17-39) — generic-as-is — deterministic path-claiming with ID-suffix collision fallback; takes `directory`, `name`, `record_id`, `taken` as opaque strings/sets, no fiction coupling.
- `build_path_index` (paths.py:42-81) — needs-generalization — signature is hardcoded to six fiction record-list parameters (`chapters, characters, world_entries, threads, secrets, themes`) and a hardcoded `("character", "characters", ...), ("world", "world", ...)` etc. tuple of `(kind, directory, records, label)`; the *body*'s per-kind loop mechanism (duplicate-ID check, `_claim` call, index assignment) is generic-as-is and would generalize immediately if the kind/directory/records/label tuples were domain-supplied rather than fiction record types spelled out in the signature.

### `novelizer/canon_fs/search.py`

- `SEARCH_RESULT_CAP` (search.py:9) — generic-as-is — a plain int constant, no fiction reference.
- `build_search_canon_tool` (search.py:12-87) — mixed: the factory/closure pattern (close over story-scoped stores, return a `@tool`-decorated function) is generic-as-is, but the returned `search_canon` tool is fiction-only in its details — its docstring names fiction kinds (chapters, characters, threads, secrets, themes, promises, chapter briefs, arcs), and `FALLBACK_PATH_BY_KIND` (search.py:63-67) hardcodes fiction-specific fallback paths (`/outline/ledger.md`, `/outline/briefs/`) for kinds with no dedicated canon_fs file. It also calls `build_path_index` directly with the six fiction record lists, inheriting paths.py's needs-generalization coupling.
- `_format_entity_hit` (search.py:90-102) — generic-as-is — formats a KG entity hit using only `kg_store.get_entity`/`entity_relations` and generic dict keys (`entity_type`, `name`, `description`, `relation_type`, `other_name`); no fiction-specific literal.

### `novelizer/canon_fs/skills_route.py`

- `_HIDDEN_ENTRY_NAMES` (skills_route.py:23) — generic-as-is — packaging artifact names (`__init__.py`, `__pycache__`), not fiction content.
- `CRAFT_SKILLS` (skills_route.py:44) — generic-as-is (mechanism) / needs-generalization (payload) — the constant itself is just `["/skills"]`, a container path; the fiction coupling lives outside this file, in what's shipped under `novelizer.skills_packs` (the five craft-writing skill packs), not in this file's logic.
- `ReadOnlyBackend` (skills_route.py:47-127) — generic-as-is — a pure `BackendProtocol` decorator that refuses writes and delegates/filters reads; no fiction reference anywhere in the class.
- `_has_hidden_segment` / `_filter_hidden_entries` / `_filter_hidden_glob_matches` (skills_route.py:130-158) — generic-as-is — packaging-artifact filtering helpers, domain-neutral.
- `build_skills_backend` (skills_route.py:161-173) — needs-generalization — hardcodes `importlib.resources.files("novelizer.skills_packs")` as the root; a generalized substrate would take the skills-pack package name as a parameter rather than importing this fiction-specific package path.

### `novelizer/store/kg_store.py`

- `_SCHEMA` (kg_store.py:6-39) — generic-as-is — table columns (`name`, `entity_type`, `description`, `canon_id`, `first_seen`, `last_seen`; `source_id`/`target_id`/`relation_type`; `event_fingerprint`) are all domain-neutral graph primitives — no fiction column name.
- `KGStore.__init__/init/close` (kg_store.py:48-59) — generic-as-is — connection lifecycle only.
- `KGStore.upsert_entity` (kg_store.py:61-80) — generic-as-is — confirmed by reading the SQL: `entity_type` is bound as a `?` parameter (kg_store.py:62-77), never a literal fiction string in the method body itself. (Fiction-specific entity-type strings like `"character"`/`"world_entry"` are supplied by *callers* — see `kg_structured.py` below — not embedded in `KGStore`.)
- `KGStore.upsert_relation` (kg_store.py:82-102) — generic-as-is — `relation_type` is likewise a bound parameter, no literal.
- `KGStore.link_mention` / `clear_mentions_for_fingerprint` / `has_mentions` (kg_store.py:104-128) — generic-as-is — operate on opaque `entity_id`/`event_fingerprint` strings.
- `KGStore.link_relation_mention` / `clear_relation_mentions_for_fingerprint` / `relation_has_mentions` / `delete_relation` (kg_store.py:130-161) — generic-as-is — same pattern for relation mentions.
- `KGStore.find_entity_by_name` / `get_entity` / `entity_relations` (kg_store.py:163-189) — generic-as-is — plain lookups/joins by opaque id/name/type; `entity_relations`'s output dict keys (`relation_type`, `other_name`, `direction`) are domain-neutral.
- Conclusion: `KGStore` in full is generic-as-is. Verified per the brief's instruction by reading every method body — none references a fiction entity/relation type as a literal; `entity_type`/`relation_type` are always `str` parameters bound into SQL, never hardcoded.

### `novelizer/store/kg_projector.py`

- `_EXTRACTION_CHUNK_CHARS` / `_EXTRACTION_CHUNK_OVERLAP` (kg_projector.py:15-16) — generic-as-is — plain ints tuning extraction-chunk size; the docstring comment contrasts this with embeddings.py's retrieval chunk size but the constants themselves are domain-neutral.
- `INDEXED_EVENT_TYPES` (kg_projector.py:18-22) — needs-generalization — literal list of fiction `EventType` constants (`CHARACTER_CREATED`, `CHARACTER_UPDATED`, `WORLD_ENTRY_CREATED`, `WORLD_ENTRY_SUPERSEDED`, `CHAPTER_CREATED`, `CHAPTER_REVISED`) that this projector subscribes to.
- `KGProjector.__init__` (kg_projector.py:40-46) — generic-as-is — stores `events`, `read_store`, `kg_store`, `embedding_store`, `extraction_runner`, `cursor_path` handles only; no fiction import in the constructor.
- `KGProjector._load_cursor` / `_save_cursor` (kg_projector.py:48-57) — generic-as-is — atomic JSON cursor-file read/write, domain-neutral (matches the class docstring's claim of being "structurally identical to store/indexer.py's CanonIndexer").
- `KGProjector.catch_up` (kg_projector.py:59-76) — generic-as-is (mechanism) — cursor-driven event replay loop with failure-tolerant catch-up; calls `self._events.events_since(last, event_types=list(INDEXED_EVENT_TYPES))`, so it inherits `INDEXED_EVENT_TYPES`'s needs-generalization coupling at the call site, but the loop body itself is domain-neutral.
- `KGProjector._index_one` (kg_projector.py:78-84) — needs-generalization — hardcoded `if event_type in (EventType.CHARACTER_CREATED, ...)` dispatch to `_index_character`/`_index_world_entry`/`_index_chapter`; a generalized substrate needs an injected event-type-to-handler mapping instead.
- `KGProjector._index_character` (kg_projector.py:86-93) — fiction-only — calls `self._read.get_character`/`list_characters` and `kg_structured.facts_from_character` by name.
- `KGProjector._index_world_entry` (kg_projector.py:95-102) — fiction-only — calls `self._read.list_world_entries` and `kg_structured.facts_from_world_entry` by name.
- `KGProjector._index_chapter` (kg_projector.py:104-154) — mixed: the reflow mechanism (clear prior fingerprint's mentions/relations, re-extract, upsert, orphan-prune check) is generic-as-is in shape, but the method calls `self._read.get_chapter`, `list_characters`, `list_world_entries` by name and builds `canon_ids_by_name` from fiction record fields (`c.name`, `e.title`) — needs-generalization for those specific calls.
- `KGProjector._extract_chapter_facts` (kg_projector.py:156-183) — mixed: the chunk-extract-merge-dedupe mechanism (chunk prose, call an LLM runner per chunk, merge/dedupe entities and relations across chunk boundaries) is generic-as-is, but it imports `novelizer.agents.kg_extraction.kg_extraction_prompt` — a fiction-specific prompt-building function — so the extraction-prompt dependency is needs-generalization.
- `KGProjector._format_entity_detail` (kg_projector.py:185-189) — generic-as-is — pure string formatting over `name`/`entity_type`/`description`/`relations` dicts.
- `KGProjector._commit_facts` (kg_projector.py:191-212) — generic-as-is — takes `entities`/`relations` as already-domain-neutral `EntityFact`/`RelationFact`-shaped objects (see kg_structured.py below) and upserts/links them via `KGStore`; no fiction literal in this method body itself, though the facts it receives originated from fiction-only producers.

### `novelizer/store/kg_structured.py`

- `EntityFact` (kg_structured.py:6-11) — generic-as-is — fields `name: str, entity_type: str, description: str, canon_id: str | None`; a domain-neutral fact-carrier dataclass.
- `RelationFact` (kg_structured.py:14-20) — generic-as-is — fields `source_name, source_type, target_name, target_type, relation_type`, all plain strings.
- `facts_from_character` (kg_structured.py:23-47) — fiction-only — takes a `Character` model, hardcodes `entity_type="character"` (kg_structured.py:34) and `source_type/target_type="character"` (kg_structured.py:43-44) as literals, reads `char.traits`/`char.relationships`.
- `facts_from_world_entry` (kg_structured.py:50-55) — fiction-only — takes a `WorldEntry` model, hardcodes `entity_type="world_entry"` (kg_structured.py:52), reads `entry.title`/`entry.body`.
- Note: the two `facts_from_*` functions are the fiction-specific *producers* that feed the generic `EntityFact`/`RelationFact` carriers into `KGStore`'s generic-as-is upsert methods — this is the same "generic carrier, fiction-specific producer" split as `Committer.commit`'s `payload: BaseModel` in the Canon Core section.

## Comparison: canon_fs vs. kg_store/kg_projector

Both projection stacks recompute a fresh view from canon, keyed to what triggered the recompute — but they differ in *when* that recompute happens and *what* it's keyed on:

- **canon_fs is pull/on-demand, keyed on the request.** `CanonBackend._snapshot` (backend.py:93-110) rebuilds a full `_Snapshot` — six record-kind dicts plus `build_path_index` and the knowledge matrix — synchronously inside every `aread`/`als`/`aglob`/`agrep` call. There is no persisted cache: each backend call is its own consistent-but-throwaway view, and `CanonBackend._render` (backend.py:112-127) dispatches by `kind` string to the fiction-only `render_*` functions in `canon_fs/render.py` to turn one record into text on the spot.
- **kg_store/kg_projector is push/event-driven, keyed on the event.** `KGProjector.catch_up` (kg_projector.py:59-76) is a cursor-driven consumer over `EventStore` (persisted cursor file, `_load_cursor`/`_save_cursor`), processing each new event through `_index_one` → `_index_character`/`_index_world_entry`/`_index_chapter`, and persisting the resulting entities/relations into `world.db` via `KGStore.upsert_entity`/`upsert_relation` (kg_store.py:61-102) plus `KGStore.link_mention`/`link_relation_mention` (kg_store.py:104-136) — a durable, incrementally-updated projection, not a per-call recompute.
- **The "keyed to what triggered it" idea shows up as two different keys.** canon_fs has no explicit trigger key at all — its snapshot is implicitly keyed on "this call happened now." kg_store makes the trigger explicit as `event_fingerprint` (e.g. `f"chapter:{chapter_id}"` at kg_projector.py:108): `KGStore.clear_mentions_for_fingerprint`/`clear_relation_mentions_for_fingerprint` (kg_store.py:112-122, 138-148) let `KGProjector._index_chapter` (kg_projector.py:104-154) retract exactly the facts a prior version of one source (one chapter revision) contributed, before re-extracting — the reflow pattern. canon_fs has no analogous retraction concept because it never persists a prior render to invalidate; every call already starts from current data.
- **Both split into a generic carrier + fiction-specific producer.** canon_fs's carrier is the rendered markdown string itself, produced by fiction-only functions (`render_chapter`, `render_character`, etc. in `render.py`; `render_blueprint`, `render_ledger`, etc. in `outline_render.py`). kg_store/kg_projector's carrier is `EntityFact`/`RelationFact` (kg_structured.py:6-20), a domain-neutral dataclass pair, produced by fiction-only functions `facts_from_character`/`facts_from_world_entry` (kg_structured.py:23-55) for the structured path, or by the fiction-specific `kg_extraction_prompt` LLM call (kg_projector.py:163, `_extract_chapter_facts`) for the prose-extraction path. In both stacks, the generic-as-is store/backend layer (`KGStore`, `CanonBackend`'s protocol dispatch) never itself names a fiction entity type as a literal; the fiction vocabulary enters only through the producer functions and the hardcoded dispatch tables (`KIND_DIRS`, `INDEXED_EVENT_TYPES`, `_index_one`'s `if` chain) that route to them.
- **Both dispatch tables are the same kind of seam.** `CanonBackend._render`'s `if kind == "chapter": render_chapter(...)` chain (backend.py:112-127) and `KGProjector._index_one`'s `if event_type in (EventType.CHARACTER_CREATED, ...): ...` chain (kg_projector.py:78-84) are structurally identical: a hardcoded string/enum-keyed if-chain routing to a fiction-only handler function. A generalized substrate would replace both with an injected `dict[str, Handler]` supplied by the domain, rather than importing named fiction functions/constants directly.

## Agent Registry, Middleware & Skills

### `novelizer/agents/registry_types.py`

- `ToolGrant` (registry_types.py:6-12) — generic-as-is — a single `enabled_setting: str` field and an `is_enabled(settings)` that does `bool(getattr(settings, self.enabled_setting))`; no fiction-specific field or literal, any domain's settings object works.
- `AgentContext` (registry_types.py:15-31) — generic-as-is — every field (`read`, `committer`, `events`, `settings`, `casting_note`, `personalities`, `provenance`, `tooled`, `runner_for`) is an opaque handle or `Callable`, typed `Any`; `casting_note`/`personalities`/`provenance` are fiction-flavored *names* but carry no fiction type — a non-fiction domain could repurpose them as "framing note" / "voice profiles" / "sourcing" with zero code change.
- `AgentSpec` (registry_types.py:34-41) — generic-as-is, confirmed against the current file rather than the design doc it's read from. The file's actual field set is `name: str`, `tool_grant: ToolGrant | None`, `construct: Callable[[AgentContext], Any]` — this has *shrunk* relative to the brief's expected anchor list (`agent_class`, `build_runner`, `interval_setting`, `extra_kwargs`, `fallback_name` don't exist on this dataclass as written). All three actual fields are domain-neutral types (str, ToolGrant, Callable); the per-agent constructor irregularity the design doc worried about is handled by giving `construct` full responsibility for the actual build, not by enumerating fields for every possible constructor shape. Domain-specific behavior (which runner shape, what kwargs, what fallback) lives inside each agent module's own `construct` closure, not in this dataclass.

### `novelizer/agents/registry.py`

- `AGENT_REGISTRY` (registry.py:11-16) — fiction-only — the literal list of ten fiction agents (`world_architect.SPEC, character_keeper.SPEC, muse.SPEC, plotter.SPEC, author.SPEC, editor.SPEC, continuity_checker.SPEC, retconner.SPEC, structure_analyst.SPEC, triage.SPEC`) in a fixed scheduling order (planner before writer, writer before critics, triage last), plus the fiction agent module imports at the top of the file. The *pattern* — a module-level `list[AgentSpec]` whose order is scheduling order, built once and iterated by `Runtime` — is what a domain's own registry module replicates verbatim; only the imported SPECs and their order are fiction-specific.

### `novelizer/agents/middleware.py`

- `_tool_name` (middleware.py:9-13) — generic-as-is — reads `.name` off a tool object or `"name"` off a dict, no fiction reference.
- `_format_todos` (middleware.py:16-19) — generic-as-is — renders a generic todo-status marker table from `status`/`content` keys; not fiction content.
- `ExcludeToolsMiddleware` (middleware.py:22-41) — generic-as-is — strips a caller-supplied `frozenset[str]` of tool names from the model request; the docstring's example (`write_todos`, "Author-only") names a fiction agent but the class itself takes `excluded` as a parameter and contains no fiction literal.
- `TodoContextMiddleware` (middleware.py:44-71) — generic-as-is — re-appends `state["todos"]` to the system prompt to survive summarization; operates purely on LangChain/deepagents' generic todo state shape, no fiction coupling.
- Correction to the brief's expected anchor: `middleware.py` does **not** contain the `CompositeBackend` wiring or any `/canon`/`/scratch` path-prefix routing — that code lives in `novelizer/runtime.py`'s `Runtime._phase_a_toolkit` (runtime.py:111-134), not in `agents/middleware.py`. Read there instead: `CompositeBackend(default=CanonBackend(self.read), routes={"/outline/": OutlineBackend(self.read), "/skills/": build_skills_backend(), "/workspace/": StateBackend()})` (runtime.py:125-132). The brief's guessed fiction-specific `/canon`/`/scratch` literals are wrong — the actual prefixes are `/outline/`, `/skills/`, `/workspace/`, routed to `OutlineBackend`, `build_skills_backend()`, and deepagents' generic `StateBackend()` respectively, with `CanonBackend(self.read)` as the unprefixed default. That single wiring expression splits into three single-label items:
  - The `CompositeBackend` dispatch mechanism and the `/workspace/` → `StateBackend()` route (runtime.py:125-132) — generic-as-is — the wiring mechanism (construct a default backend plus a dict of path-prefix-routed backends, imported from `deepagents.backends.CompositeBackend`) is domain-neutral: `CompositeBackend` itself is an upstream deepagents primitive, the routing *shape* (prefix string → backend instance) is generic, and `/workspace/` routes to deepagents' own generic `StateBackend()` with no fiction coupling.
  - The `/outline/` route and the bare `default=CanonBackend(self.read)` fallback (runtime.py:125-132) — fiction-only — `/outline/` and the unprefixed default routing to `CanonBackend` are fiction-specific path conventions naming this domain's own record types.
  - The `/skills/` route, `build_skills_backend()` (runtime.py:125-132) — needs-generalization — it inherits `skills_route.py`'s needs-generalization coupling (documented under Projections) since `build_skills_backend()` hardcodes the `novelizer.skills_packs` package path.

### `novelizer/skills_packs/__init__.py` and `novelizer/skills_packs/outlining/`

- `novelizer/skills_packs/__init__.py` — empty (a bare package marker, zero lines). There is no loader mechanism in this file at all — the actual loader is `build_skills_backend`/`ReadOnlyBackend` in `novelizer/canon_fs/skills_route.py` (already classified under Projections above), which walks `importlib.resources.files("novelizer.skills_packs")` for `SKILL.md`-shaped entries. That loader is generic-as-is *mechanism* (directory walk + read-only filtering, no fiction reference) with one needs-generalization seam: the hardcoded `"novelizer.skills_packs"` package path (skills_route.py:161-173, already noted in the Projections section) — a generalized substrate would take the skills-pack package name as a parameter.
- `novelizer/skills_packs/outlining/SKILL.md` and its `references/beat-frameworks.md`, `references/obligatory-scenes.md` — fiction-only (pack content). The pack teaches story-structure craft as data: adopting a `BlueprintPlan` framework (`"six-position"` vs `"kishotenketsu"`, keyed to `novelizer.canon.beat_templates.BEAT_TEMPLATES`), drafting rolling-wave `BriefIntent`s, and fulfilling `BeatIntent`s against `ideal_pct`/`tolerance_pct` windows — every noun here (blueprint, beat, brief, obligatory scene, Story Grid/Save the Cat/Seven-Point/Story Circle frameworks) is fiction craft vocabulary with no domain-neutral residue. The sibling packs (`character-arcs`, `pacing`, `promise-payoff`, `scene-sequel`) are structurally identical in kind — each is a `SKILL.md` plus `references/` markdown, fiction-only content loaded by the same generic-as-is directory-walk mechanism.
- Split confirmed: the *loader* (walk a directory for `SKILL.md`-shaped files, expose read-only) is generic-as-is (modulo the one hardcoded package-name seam); the *pack content itself* is fiction-only in every pack inspected, since these are craft-writing skills (outlining, character arcs, pacing, promise/payoff, scene/sequel) with no equivalent in a non-fiction domain.
